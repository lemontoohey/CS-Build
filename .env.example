# Copy this file to .env and fill in your own values.
# .env is gitignored — never commit real keys.

# --- AI receipt parsing ---
# You don't have to set these here at all — the easier way is to run the app
# and add your API key on the Settings page (/settings), which works for
# whoever is actually using this build regardless of which provider they use.
# These env vars are just a fallback/default if you'd rather set it once here.
#
# AI_PROVIDER=anthropic          # or: openai
# ANTHROPIC_API_KEY=             # from https://console.anthropic.com
# ANTHROPIC_MODEL=claude-sonnet-5
# OPENAI_API_KEY=                # from https://platform.openai.com
# OPENAI_MODEL=gpt-4o

# --- Data backend ---
# By default this app stores everything in a local SQLite file
# (data/app.db) — zero setup, nothing leaves this machine.
#
# To use Supabase (Postgres) instead — so data is available from any
# machine that runs this app, and to make it easy to hand off to someone
# else — set BOTH of these. Get them from your Supabase project's
# Settings > API page. Also run supabase/schema.sql once in the Supabase
# SQL Editor to create the tables before switching this on.
#
# SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
# SUPABASE_KEY=                  # the "anon" / "publishable" key, not the service_role key

# Optional — defaults to 3000 if not set.
# PORT=3000
